id: composite-source

vars:
  file-uri: file://
  any: _any_

uri: "{{ file-uri }}source-composite.so"

outputs:
  - id: source-composite-out-1
    type: "{{ any }}"
  - id: source-composite-out-2
    type: "{{ any }}"

tags: []
